spring:
  application:
    name: inventory-service
  datasource:
    url: jdbc:mysql://localhost:3306/gotrack_db?createDatabaseIfNotExist=true
    username: root
    password: "root2552005"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect