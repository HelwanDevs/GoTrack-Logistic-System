spring:
  application:
    name: support-and-notifications-service
  mongodb:
      uri: mongodb+srv://goTrackNotificationUsername:u-EvhFaiBUetCJ3@gotrack.pefntiy.mongodb.net/notifications

  docker:
    compose:
      enabled: false

eureka:
  client:
    enabled: false

server:
  port: 8080